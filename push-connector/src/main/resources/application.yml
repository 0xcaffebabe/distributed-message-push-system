connector:
  port: ${cport}
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///message
    username: root
    password: root
  cloud:
    nacos:
      discovery:
        server-addr: local:8848
  application:
    name: connector-service
  redis:
    host: local
server:
  port: ${sport}